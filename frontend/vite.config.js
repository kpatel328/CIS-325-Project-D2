import { defineConfig } from 'vite';

// Define and export Vite configuration
export default defineConfig({
  // Configuration options here (if any)
  server: {
    port: 1234, // Port to run the Vite dev server on
  },
});